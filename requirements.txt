# This repository uses npm for dependency management (backend & frontend).
#
# The original file listed npm packages which are invalid Python package
# specifiers. Many CI hosts automatically run `pip install -r requirements.txt`
# if this file exists. That caused the error:
#
#   Invalid requirement: '@types/cors==2.8.19': Expected package name at the start
#
# To avoid CI trying to pip-install npm packages, this file is intentionally
# minimal and documents where Node dependencies live.
#
# Backend dependencies:  see `backend/package.json`
# Frontend dependencies: see `frontend/package.json`
#
# To install locally:
#   cd backend; npm install
#   cd frontend; npm install
#
# If you do need Python packages for a build step, list them below (one per
# line), for example:
# requests==2.31.0

